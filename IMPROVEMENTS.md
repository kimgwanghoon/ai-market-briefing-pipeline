# 프로젝트 개선 기록

## 이번 개선에서 해결한 항목

- 장중·주간 작업이 매번 빈 `public/data`에서 시작하던 문제를 수정했다. 배포된 히스토리를 복원한 후 계산한다.
- 네 개의 Pages 쓰기 워크플로를 공통 concurrency group으로 직렬화했다.
- 주간 리포트의 0~100 점수와 과거 -100~100 점수 스키마 불일치를 교정하고 구버전 데이터를 변환한다.
- 뉴스·공시 등 외부 문자열을 비신뢰 JSON 데이터로 격리하고 AI 응답을 strict JSON Schema로 제한했다.
- 데일리 요약과 헤드라인을 한 번의 구조화 호출로 합쳐 형식 재시도와 비용을 줄였다.
- Jinja autoescape와 escape-first 굵기 필터를 적용해 외부 데이터와 모델 출력의 저장형 XSS 경로를 차단했다.
- 데이터가 없을 때도 35%로 표시되던 품질 점수 하한과 이벤트 없는 섹터의 과대 확인을 제거했다.
- 주간 리포트가 긍정 이벤트를 리스크로 표시하던 문제를 고치고 리스크·기회 항목을 분리했다.
- KST 파일명을 UTC로 잘못 해석하던 보존기간 계산과 삭제 실패의 성공 종료를 수정했다.
- 정적 페이지의 커버 경로, 문서 제목 구조, 내비게이션, 모바일 툴팁, 빈 상태를 정리했다.
- 회귀 테스트와 GitHub Actions 품질 검사를 추가했다.
- 이벤트 ID 기준으로 이전 스냅샷과 중복되는 뉴스·공시의 재채점을 막았다.
- 적중률을 동일 거래일의 20~120분 후 실제 지수 레벨 수익률로 재정의했다.
- AI 문장마다 `claim_type`과 `evidence_ids`를 요구하고 지표별 숫자·결측·인용 관계를 검증한다.
- 필수 국내 지수 또는 최소 주간 표본이 없으면 기존 정상 페이지를 덮어쓰지 않도록 배포를 중단한다.

## 다음 우선순위

1. 지수별 `source`, `observed_at`, `market_session`, `stale`, 수집 오류를 스냅샷에 기록한다.
2. 뉴스 제공 시각을 표준화해 실제 관측 구간을 표시하고, 시간 정보가 없는 DART 데이터의 범위를 별도로 노출한다.
3. AI 프롬프트·모델 변경을 실제 고정 fixture로 비교하는 정성 eval을 추가한다.
4. Python 의존성과 GitHub Action을 검증된 해시로 고정한다.
